namespace ClothingPlanner.Services;

using ClothingPlanner.Models;

public class ScrapperService
{
    public Clothing ScrapeLinkForClothing(Uri link)
    {
        // I think we need to determine several things from the link

        // See if it is an accepted website

        // Scrape for the specific site 

        return new Clothing();
    }
}